#include <iostream>

int main()
{
char array[100];
std::cin >> array;
int number;
std::cin >> number;
  if ((array[number-1] >= 48) && (array[number-1] <= 57)) {
    std::cout << "Yes, it is digit";
  } else {
    std::cout << "No, it isn't";
  }
}
